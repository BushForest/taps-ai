# SENTRY_DSN=https://...@o0.ingest.sentry.io/0  # Optional: enable Sentry error reporting

# SENTRY_AUTH_TOKEN — CI/CD build secret only; set in Vercel project settings and in the CI workflow
# environment (Railway dashboard → service → Variables). Never commit to source.
# SENTRY_ORG=tapseats
# SENTRY_PROJECT=taps-api
